node_modules/
out/
dist/
release/
.vite/
*.log
.tmp/
tests/.artifacts/
.DS_Store

# Tools fetched by `npm run tools` — downloaded, never committed.
resources/bin/*
!resources/bin/README.md

# TypeScript incremental build cache.
*.tsbuildinfo

# Transfer/archive scratch — not source.
.xfer/

# Quarantine for files pending deletion — never source.
_to_delete/

# Agent/editor tooling installed alongside the project, not project source.
.agents/
.claude/
.viz/
